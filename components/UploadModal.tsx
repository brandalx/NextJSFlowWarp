import Modal from "./Modal";
const UploadModal = () => {
  return (
    <Modal
      title="Upload modal"
      description="Upload modal"
      isOpen
      onChange={() => {}}
    >
      Upload content
    </Modal>
  );
};

export default UploadModal;
